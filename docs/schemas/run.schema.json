{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/danilo-aguiar-br/browser-automation-cli/docs/schemas/run.schema.json",
  "title": "run command input",
  "type": "object",
  "description": "Run a multi-step script in one process (NDJSON or JSON array of steps)",
  "properties": {
    "script": {
      "argv": "--script",
      "description": "Path to the script file: NDJSON (one step object per line) or a top-level JSON array of step objects",
      "required": true,
      "source": "parser",
      "step_key": "script",
      "surfaces": [
        "argv"
      ],
      "type": "string"
    }
  },
  "required": [
    "script"
  ],
  "additionalProperties": false
}